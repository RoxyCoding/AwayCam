# ワンクリック起動ランチャー仕様

## スコープ

- Windowsで `run.py` やコマンドを入力せず、ファイルのダブルクリックだけでAwayCamを起動する。
- 起動時にコンソールウィンドウを表示しない。
- 必要な仮想環境やエントリポイントがない場合は、日本語で修復方法を案内する。

## 受け入れ条件

1. `AwayCam.vbs` のダブルクリックで `.venv\Scripts\pythonw.exe` と `run.py` が起動する。
2. 作業フォルダはプロジェクト直下に設定され、相対パスに依存する機能が維持される。
3. パスに空白が含まれても起動できる。
4. `cscript //nologo AwayCam.vbs check` で、アプリを起動せず構成を検査できる。

## 非対象

- Pythonや依存パッケージを含む単体EXEの生成。
- デスクトップやスタートメニューへのショートカット自動作成。

