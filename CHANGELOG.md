# 変更履歴

## 未リリース

- PC をスリープ / 休止から復帰させた後に動作しなくなる問題を修正。
  - Windows の電源通知（WM_POWERBROADCAST）を受け取り、眠る前にカメラを
    解放し、復帰したら開き直すようにした。
  - 電源通知が届かない場合の保険として、実時計の飛びからも復帰を検知する。
  - 止まっていた時間で離席判定が進まないようにした。
  - 判定ループが例外で静かに停止しないようにし、例外をログに残すようにした
    （pythonw 起動では標準エラーの出力先が無く、原因が追えなかった）。

## 1.0.1 - 2026-08-24

- `AwayCam.vbs` のダブルクリックだけで起動できるワンクリックランチャーを追加。
- コンソールを表示しない `pythonw.exe` 起動に対応。
- 仮想環境や `run.py` が見つからない場合の日本語エラー案内を追加。

